import App from "../App";

function Layout() {
  return (
    <div
      style={{
        height: "100vh",
        width: "100%",
      }}
    >
      <App />
    </div>
  );
}

export default Layout;
